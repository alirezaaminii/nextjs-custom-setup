import type { NextPage } from 'next'
import Head from 'next/head'

const Home: NextPage = () => (
    <div>
      <Head>
        <title>Armani Dashboard</title>
        <meta name="description" content="Armani Dashboard" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
    </div>
  )

export default Home
